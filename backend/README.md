# Backend API — AI Architecture Assistant

This backend uses **FastAPI** to provide a `/chat` endpoint that connects
the user interface to the AI-driven Architecture Assistant.

### Endpoint: POST /chat

Request:
```json
{
  "message": "generate architecture for hospital system"
}

Response:
 {
  "response": "Here is a 3-tier architecture for a hospital system..."
}


Commit.

---




we will run our chatbot from a small Python script to confirm everything works 🔥


